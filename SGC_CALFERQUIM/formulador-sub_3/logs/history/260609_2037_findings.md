# Technical Finding / Milestone

**Context**: Sesión de continuación del formulador CFQ. Implementación de Fases 4-7 del plan integrado.

## The Discovery

Se conectó exitosamente Convex en modo local, se cargó el CSV completo (296 items), se corrigieron errores de tipo críticos (`v.optional` en `returns`, `Id<...>` vs `string`), y se implementaron las funciones avanzadas de Fase 7: creación manual de productos, comparador de snapshots y sustitución con sugerencias nutricionales basadas en similitud coseno.

## Key Files Affected

- `web/convex/catalog.ts` — Agregada `createCatalogItem` con generación secuencial de IDs y auditoría.
- `web/convex/lists.ts` — Agregada `compareSnapshots` con diff de componentes y composición.
- `web/convex/catalog.ts` — Corregido `getCatalogItem` para usar `v.union(v.null(), v.object(...))`.
- `web/convex/lists.ts` — Corregido `getProductList`, `getSnapshot`, `saveProductList` y `cloneSnapshotToList` para tipos `Id` correctos.
- `web/lib/calculation.ts` — Agregado `MINIMOS_NPK` y `suggestAlternatives` (similitud coseno).
- `web/src/pages/FormulatePage.tsx` — Autosave debounce, barra de progreso kg, filtro MP, sugerencias de sustitución, alerta NPK.
- `web/src/pages/CatalogPage.tsx` — Modal de creación manual de PT/MZR solo para admin.
- `web/.env.local` — Configurado con `VITE_CONVEX_URL` y `CONVEX_DEPLOYMENT` para local.

## Tests

- `web/lib/calculation.test.ts` — 6 tests.
- `web/lib/tolerancia.test.ts` — 5 tests.
- Todos pasan con `npx vitest run`.

## Build

- `npx vite build` exitoso.
- Warnings de `lightningcss` sobre `@theme` (Tailwind v4) — no bloqueantes.

## Pruebas de Integración (CLI)

- `seedCatalog` — 296 filas insertadas, 0 errores.
- `createCatalogItem` — Creó `PT0269` con nutrientes correctos.
- `compareSnapshots` — Diferenció v1 vs v2 de lista de prueba (`TEST-L001`) correctamente.
