# Technical Finding / Milestone

**Context**: Configuracion real de Convex y conexion inicial de React.

## The Discovery

Se creo un proyecto Convex nuevo para el formulador bajo el team `wilson-salas`:

- Proyecto: `formulador-cfq`
- Deployment dev: `dev:valiant-stingray-191`
- Dashboard: `https://dashboard.convex.dev/d/valiant-stingray-191`
- URL cliente: `https://valiant-stingray-191.convex.cloud`

El CLI genero `.env.local` y `web/convex/_generated/`, aplico los indices definidos en `schema.ts` y dejo las funciones Convex listas.

La UI React ahora se envuelve con `ConvexProvider` cuando existe `VITE_CONVEX_URL` y usa queries/mutations Convex para catalogo, auditoria, listas y snapshots. Se dejo una pantalla de configuracion faltante para evitar errores de hooks si falta `.env.local`.

## Key Files Affected

- `web/.env.local`: deployment y URLs Convex.
- `web/convex/_generated/*`: tipos/API generados por Convex.
- `web/convex/tsconfig.json`: configuracion generada por Convex.
- `web/src/main.tsx`: `ConvexProvider`, queries/mutations y mapeo remoto/local.
- `web/src/style.css`: pantalla de configuracion faltante.

## Verification

- `pnpm test`: 10 pruebas pasan.
- `pnpm build`: pasa.
- `curl -I http://localhost:5160/`: HTTP 200.
- Servidor Vite activo en `http://localhost:5160/`.
