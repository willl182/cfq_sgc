# Problem Record (Troubleshooting)

**Severity**: Medium

## The Problem

Comando sed falló al intentar eliminar bloques de firmas de los 13 documentos en `poe_rev/`. El bloque de firmas tiene un formato específico con "_______________________" y espacios.

## User-Requested Corrections

Usuario solicitó NO incluir bloques de firmas al final de los documentos (instrucción explícita).

## The Solution / Workaround

Aún no resuelto. Se requiere:
1. Usar un método diferente a sed (quizás editar manualmente cada archivo)
2. O usar un script más robusto que maneje el formato específico de las firmas

## Learnings to Preserve

- El formato de las firmas tiene líneas con "_______________________" y espacios en blanco entre ellas
- Sed puede tener problemas con caracteres especiales y newlines
- Considerar editar manualmente o usar otro enfoque (Python, awk, etc.)
